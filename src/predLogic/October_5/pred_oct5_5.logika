A x (P(x) -> Q(x) V K(x)), A y (P(y)) |- A n (Q(n) V K(n))
{
    1. A x (P(x) -> Q(x) V K(x))            premise
    2. A y (P(y))                           premise
    3. {
        4. a
        5. P(a) -> Q(a) V K(a)              Ae 1 a
        6. P(a)                             Ae 2 a
        7. Q(a) V K(a)                      ->e 5 6
        //want: Q(a) V K(a)
    }
    //use Ai
    8. A n (Q(n) V K(n))                    Ai 3
}