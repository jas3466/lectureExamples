∀x (Q → P(x)) ⊢ Q → (∀x P(x))
{
    1. A x (Q -> P(x))              premise
    2. {
        3. Q                        assume
        4. {
            5. a
            6. Q -> P(a)            Ae 1 a
            7. P(a)                 ->e 6 3
        }
        8. A x P(x)                 Ai 4
        //I want to get to A x P(x)
    }
    9. Q -> (A x P(x))              ->i 2
}